---
import site from "../data/site.json";
---

<section class="hero">
  <div class="hero-bg" aria-hidden="true">
    <span class="orb orb-1"></span>
    <span class="orb orb-2"></span>
  </div>

  <div class="wrap hero-inner">
    <div class="hero-copy">
      <p class="eyebrow" data-reveal>West Linn, Oregon &middot; ZIP {site.zip}</p>
      <h1 data-reveal style="--reveal-delay: 80ms">
        Know the
        <span class="script">neighborhood</span>
        before you know the home.
      </h1>
      <p class="hero-lead measure" data-reveal style="--reveal-delay: 180ms">
        Lewis Realtors is Melissa Shaw's West Linn, Oregon real estate office, with 20+ years of
        local experience and more than $108 million sold since 2016. Each of the 11 West Linn
        neighborhoods (ZIP 97068) gets its own free monthly market report.
      </p>
      <div class="hero-actions" data-reveal style="--reveal-delay: 280ms">
        <a class="btn btn--gold btn--lg" href="#market-report">Get your market report</a>
        <a class="btn btn--ghost btn--lg" href="/home-value">What is my home worth?</a>
      </div>
      <ul class="hero-proof" role="list" data-reveal style="--reveal-delay: 380ms">
        {site.agent.stats.map((s) => (
          <li>
            <span class="proof-val">{s.value}</span>
            <span class="proof-label">{s.label}</span>
          </li>
        ))}
      </ul>
    </div>

    <figure class="hero-figure" data-reveal style="--reveal-delay: 220ms">
      <div class="hero-card">
        <div class="hero-photo" role="img" aria-label="West Linn home, photography to be added">
          <span class="tag-sample">Photo placeholder</span>
        </div>
        <figcaption class="hero-meta">
          <div>
            <p class="meta-name">{site.agent.name}</p>
            <p class="meta-role">{site.agent.role}</p>
          </div>
          <a class="meta-phone" href={site.contact.phoneHref}>{site.contact.phone}</a>
        </figcaption>
      </div>
      <p class="hero-mark script">Sold</p>
    </figure>
  </div>
</section>

<style>
  .hero {
    position: relative;
    /* fill one screen below the sticky header so the whole hero is above the fold */
    min-height: calc(100svh - 98px);
    display: flex;
    align-items: center;
    padding-block: clamp(1.25rem, 0.5rem + 2.5vw, 2.75rem);
    overflow: hidden;
  }
  .hero-bg { position: absolute; inset: 0; z-index: -1; }
  .orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(70px);
    opacity: 0.5;
  }
  .orb-1 {
    width: 40vw; height: 40vw;
    top: -10%; right: -8%;
    background: radial-gradient(circle, rgba(200,160,74,0.5), transparent 70%);
  }
  .orb-2 {
    width: 40vw; height: 40vw;
    bottom: -18%; left: -12%;
    background: radial-gradient(circle, rgba(108,130,165,0.55), transparent 70%);
    opacity: 0.7;
  }

  .hero-inner {
    width: 100%;
    display: grid;
    grid-template-columns: 1.05fr 0.95fr;
    align-items: center;
    gap: clamp(2rem, 1rem + 4vw, 5rem);
  }
  .hero h1 {
    margin-block: 1rem 0;
    font-size: clamp(2.4rem, 1.5rem + 3.4vw, 4.4rem);
  }
  .hero h1 .script {
    display: inline-block;
    font-size: 1.15em;
    line-height: 0.85;
    padding-inline: 0.06em;
  }
  .hero-lead {
    margin-top: 1.3rem;
    font-size: var(--fs-lead);
    color: var(--ink-soft);
    font-weight: 300;
  }
  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.9rem;
    margin-top: 1.8rem;
  }
  /* equal width, side by side; wrap to stack only when the column is too narrow */
  .hero-actions .btn {
    flex: 1 1 240px;
    padding-inline: 1.2em;
    letter-spacing: 0.12em;
  }
  .hero-proof {
    display: flex;
    flex-wrap: wrap;
    gap: clamp(1.25rem, 1rem + 2vw, 2.75rem);
    margin-top: 2rem;
    padding-top: 1.6rem;
    border-top: 1px solid var(--line);
  }
  .proof-val {
    display: block;
    font-family: var(--font-display);
    font-size: clamp(1.7rem, 1.4rem + 1.2vw, 2.3rem);
    font-weight: 600;
    color: var(--gold-deep);
    line-height: 1;
  }
  .proof-label {
    display: block;
    margin-top: 0.5rem;
    font-size: 0.72rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--ink-muted);
    max-width: 18ch;
  }

  .hero-figure { position: relative; margin: 0; justify-self: end; align-self: start; width: 100%; max-width: 400px; }
  .hero-card {
    background: var(--bg-elevated);
    border: 1px solid var(--line);
    border-radius: var(--radius-lg);
    padding: 0.9rem;
    box-shadow: var(--shadow-lg);
  }
  .hero-photo {
    position: relative;
    aspect-ratio: 5 / 6;
    max-height: 54svh;
    border-radius: var(--radius);
    background:
      linear-gradient(135deg, rgba(108,130,165,0.18), rgba(200,160,74,0.16)),
      var(--cream-deep);
    display: grid;
    place-items: center;
  }
  .hero-meta {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding: 1.1rem 0.6rem 0.4rem;
  }
  .meta-name { font-family: var(--font-display); font-size: 1.4rem; font-weight: 600; }
  .meta-role { font-size: 0.7rem; letter-spacing: 0.12em; text-transform: uppercase; color: var(--ink-muted); }
  .meta-phone { font-weight: 500; color: var(--gold-deep); font-size: 0.95rem; }
  .hero-mark {
    position: absolute;
    top: 0.6rem; right: 0.4rem;
    z-index: 2;
    font-size: clamp(3.2rem, 2.4rem + 3.6vw, 5.4rem);
    opacity: 0.96;
    transform: rotate(30deg);
    filter: drop-shadow(0 6px 14px rgba(26, 24, 20, 0.4));
    pointer-events: none;
  }

  @media (max-width: 900px) {
    .hero {
      min-height: auto;
      padding-block: clamp(2rem, 1.5rem + 3vw, 3.5rem);
    }
    .hero-inner { grid-template-columns: 1fr; }
    .hero-photo { max-height: none; }
    .hero-figure { justify-self: center; align-self: auto; margin-top: 2rem; max-width: 360px; }
    .hero-mark { top: 0.8rem; right: 0.6rem; }
  }

  /* short desktop viewports: compress so it still clears the fold */
  @media (min-width: 901px) and (max-height: 780px) {
    .hero h1 { font-size: clamp(2.1rem, 1.4rem + 2.4vw, 3.4rem); }
    .hero-lead { margin-top: 0.9rem; font-size: 1.05rem; }
    .hero-actions { margin-top: 1.3rem; }
    .hero-proof { margin-top: 1.4rem; padding-top: 1.1rem; }
    .hero-photo { max-height: 50svh; }
  }
</style>
