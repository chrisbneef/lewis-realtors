---
const blocks = [
  {
    key: "Buy",
    title: "Buy",
    copy: "Tour West Linn with peace of mind, backed by local market expertise and insights you won't find in a listing or from simply visiting a home.",
    href: "/services#buyer-representation",
  },
  {
    key: "Sell",
    title: "Sell",
    copy: "No one-size-fits-all marketing here. We tailor every strategy using more than sold data, incorporating the real-time market activity and neighborhood insights that only come from being in the field every day.",
    href: "/home-value",
  },
  {
    key: "Refer",
    title: "Refer",
    copy: "Your referral is the greatest compliment we could receive and will always be treated like our own family. Never any pressure, just service, simply connect us here.",
    href: "/contact",
  },
];
---

<section class="bsr section--ink" aria-label="Buy, sell, or refer">
  <div class="bsr-grid">
    {blocks.map((b, i) => (
      <a class="bsr-block" href={b.href} data-reveal style={`--reveal-delay: ${i * 90}ms`}>
        <span class="bsr-index">0{i + 1}</span>
        <h2 class="bsr-title">{b.title}</h2>
        <p class="bsr-copy">{b.copy}</p>
        <span class="link bsr-link">Start here <span class="arrow">&rarr;</span></span>
      </a>
    ))}
  </div>
</section>

<style>
  .bsr { padding: 0; }
  .bsr-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
  }
  .bsr-block {
    position: relative;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    padding: clamp(3rem, 2rem + 4vw, 5.5rem) clamp(1.6rem, 1rem + 2vw, 3rem);
    border-right: 1px solid var(--line-onDark);
    overflow: hidden;
    isolation: isolate;
    transition: background var(--dur) var(--ease);
  }
  .bsr-block:last-child { border-right: 0; }
  .bsr-block::before {
    content: "";
    position: absolute;
    inset: 0;
    z-index: -1;
    background: var(--grad-gold);
    transform: translateY(100%);
    transition: transform 0.6s var(--ease-out);
  }
  .bsr-block:hover::before { transform: translateY(0); }
  .bsr-block:hover { color: var(--noir); }
  .bsr-block:hover .bsr-title,
  .bsr-block:hover .bsr-index,
  .bsr-block:hover .bsr-link { color: var(--noir); }

  .bsr-index {
    font-size: 0.72rem;
    letter-spacing: 0.3em;
    color: var(--gold-bright);
    transition: color var(--dur) var(--ease);
  }
  .bsr-title {
    font-size: clamp(2.4rem, 1.8rem + 3vw, 4rem);
    color: var(--ink-onDark);
    line-height: 1;
  }
  /* The three blurbs differ a lot in length (Melissa's own wording), so reserve
     a common block height instead of letting each column size to its text. Keeps
     the "Start here" links on a shared baseline without trimming her copy. */
  .bsr-copy { color: var(--ink-onDark-soft); max-width: 34ch; flex-grow: 1; min-height: 7.5em; }
  .bsr-block:hover .bsr-copy { color: color-mix(in srgb, var(--noir) 78%, transparent); }
  .bsr-link::after { display: none; }

  @media (max-width: 760px) {
    .bsr-grid { grid-template-columns: 1fr; }
    .bsr-block { border-right: 0; border-bottom: 1px solid var(--line-onDark); }
  }
</style>
