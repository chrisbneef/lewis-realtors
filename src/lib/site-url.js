// Single source of truth for the deployed origin. Used by astro.config.mjs
// (site = canonical + sitemap URLs) and src/lib/schema.js (JSON-LD @id/url).
// Update this one line when the production custom domain is connected.
export const SITE_URL = "https://lewis-realtors.vercel.app";
